{
  "name": "golazo",
  "private": true,
  "version": "0.1.0",
  "description": "GOLAZO — bet the play. Live, in-the-moment parimutuel betting on discrete game events.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @golazo/core && npm run test --workspace @golazo/feed",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "feed": "npm run dev --workspace @golazo/feed",
    "mobile": "npm run start --workspace @golazo/mobile"
  },
  "engines": {
    "node": ">=18"
  }
}
